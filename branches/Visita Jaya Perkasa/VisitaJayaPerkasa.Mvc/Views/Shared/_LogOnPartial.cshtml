@if(Request.IsAuthenticated) {
    <text>Welcome <b>@Context.User.Identity.Name</b>!
    [ @Html.ActionLink("Log Off", "LogOff", "LogIn") ]</text>
}
else {
    @:[ @Html.ActionLink("Log On", "LogOn", "LogIn") ]
}
